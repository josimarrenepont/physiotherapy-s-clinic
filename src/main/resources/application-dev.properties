# Configurações do banco de dados
spring.datasource.url=jdbc:postgresql://localhost:5432/physiotherapys_clinic
spring.datasource.username=postgres
spring.datasource.password=07111519
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configurações do JWT
security.jwt.token.secret-key=53cr37
security.jwt.token.expire-length=3600000

# Configurações de segurança
security.allowed.urls=/auth/signing,/auth/refresh/**,/localhost:5173/refresh/**,/v3/api-docs/**

